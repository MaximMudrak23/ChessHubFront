import type { User } from "@/types/user.types";

export const mockUser: User = {
    id: 'zxc',
    name: 'TAB ON ME',
    email: 'max@test.com',
    elo: 1000,
    role: 'user',
    boardTheme: 'water',
    menuBackground: 'default',
}