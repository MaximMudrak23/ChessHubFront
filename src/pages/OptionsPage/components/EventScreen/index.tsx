import s from './styles.module.scss'

export default function EventScreen() {
    return (
        <section className={s.event_screen}>
            <p>Unfortunately now its on beta, so no options yet</p>
        </section>
    )
}